# 회고

## 진행한 내용

이번 과제에서는 콘솔에서 동작하는 음료수 자판기 프로그램을 구현했다.

사용자가 음료를 선택하고 금액을 투입하면, 선택한 음료의 가격을 차감한 뒤 거스름돈을 계산해 출력하는 방식으로 만들었다. 거스름돈은 50000원권, 10000원권, 5000원권, 1000원권, 500원 동전, 100원 동전 순서로 계산하도록 했다.

## 설계 과정

처음에는 단순히 `main` 메서드 안에 모든 로직을 작성할 수도 있었지만, 과제에서 클래스 설계도와 상속 관계를 요구했기 때문에 역할을 나누는 방식으로 설계했다.

상품의 공통 정보는 `Product` 클래스에 두고, 음료의 추가 정보는 `Drink` 클래스에 두었다. 실제 자판기에서 판매되는 음료는 `Soda` 클래스로 표현했다. 이를 통해 `Product → Drink → Soda` 구조의 2차 상속 관계를 만들었다.

자판기 실행 흐름은 `VendingMachine` 클래스가 담당하도록 했고, 거스름돈 계산은 `ChangeCalculator` 클래스로 분리했다. `Main` 클래스는 프로그램 실행 시작점 역할만 하도록 구성했다.

## 어려웠던 점

코드를 작성하는 것보다 클래스 설계도를 먼저 작성하는 과정이 더 어렵게 느껴졌다.

처음에는 속성, 메서드, 상속 관계를 다이어그램에 어떻게 표현해야 하는지 헷갈렸다. 특히 어떤 값을 부모 클래스에 두고, 어떤 값을 자식 클래스에 두어야 하는지 고민이 있었다.

예를 들어 모든 음료가 공통으로 가지는 이름과 가격은 `Product`에 두고, 음료에 해당하는 용량은 `Drink`에 두는 식으로 정리하면서 상속 구조를 이해할 수 있었다.

## 아쉬운 점

과제 요구사항은 만족했지만, 프로그램 자체는 비교적 단순하게 구현되었다.

콘솔 프로그램은 어떤 종류라도 가능하다고 되어 있어서 음료수 자판기를 선택했지만, 다른 사람들의 과제를 보면 내 과제의 완성도는 너무 쉬웠던 게 아닌가 싶은 생각이 들었다.

현재 프로그램은 음료를 한 번 구매하면 바로 종료된다. 실제 자판기처럼 계속 구매하거나, 음료별 재고를 관리하거나, 품절 상태를 처리하는 기능은 아직 없다.

## 배운 점

간단한 기능이라도 클래스를 어떻게 나누느냐에 따라 코드 구조가 달라진다는 것을 느꼈다.

처음에는 자판기 프로그램이 단순해 보여서 금방 끝날 것이라고 생각했지만, 설계도를 작성하면서 각 클래스가 어떤 책임을 가져야 하는지 생각해야 했다. 이 과정에서 코드를 작성하기 전에 구조를 먼저 고민하는 것이 중요하다는 점을 알게 되었다.

또한 상속을 사용할 때는 단순히 조건을 맞추기 위해 클래스를 나누는 것이 아니라, 공통 속성과 구체적인 속성을 구분해서 배치해야 한다는 점을 배웠다.

## 개선 방향

다음에 개선한다면 반복 구매 기능을 추가하고 싶다. 사용자가 종료를 선택하기 전까지 계속 음료를 구매할 수 있도록 만들면 실제 자판기와 더 비슷한 흐름이 될 것 같다.

또한 음료마다 재고를 두고, 구매할 때마다 재고가 줄어들도록 만들 수 있다. 재고가 0개가 되면 품절 메시지를 출력하는 기능도 추가할 수 있다.

입력값 검증도 보완하고 싶다. 현재는 숫자 입력을 기준으로 동작하지만, 문자를 입력했을 때의 예외 처리까지 추가하면 더 안정적인 프로그램이 될 것 같다.

## 정리

이번 과제는 기능 자체는 단순했지만, 클래스 설계와 상속 관계를 직접 정리해보는 데 의미가 있었다.

특히 `Product`, `Drink`, `Soda`로 이어지는 2차 상속 구조를 설계하면서 부모 클래스와 자식 클래스의 역할을 나누는 방법을 연습할 수 있었다.

다음 과제에서는 단순히 동작하는 프로그램을 만드는 것에서 끝내지 않고, 기능 확장과 예외 처리까지 고려해서 더 완성도 있게 구현해보고 싶다.
